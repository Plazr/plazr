<div class='nested-fields'>
	<%= f.association :store_category, :collection => StoreCategory.where("parent IS NOT ?",nil), :prompt => 'Choose an existing category' %>
	<%= link_to_remove_association "Remove", f %><br>
</div>